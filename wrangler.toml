name = "dianfeidianliang"
compatibility_date = "2024-01-01"

# KV 命名空间绑定（部署后在 Cloudflare 控制台创建 KV，然后填入 id）
# [[kv_namespaces]]
# binding = "AUTH_KV"
# id = "你的KV命名空间ID"

# 环境变量
[vars]
# RESEND_API_KEY 和 EMAIL_FROM 在 Cloudflare Pages 控制台的 Settings > Environment variables 中设置
# 不要写在这里！